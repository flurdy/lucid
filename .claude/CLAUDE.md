# CLAUDE Agent Behavior Guidelines

Before responding to any prompt:

1. Load and review `AGENTS.md` in the project root
2. Treat it as the authoritative source of agent behavior
3. Follow its rules strictly

Do not proceed if `AGENTS.md` is missing.
